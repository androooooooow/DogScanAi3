<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firstapp.dogscanai" >
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Camera is optional; app can run without it -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DogScanAI"
        android:usesCleartextTraffic="true" >
        <activity
            android:name="fragment_activity.MainActivity"
            android:exported="false" />
        <!-- 1. LoginActivity as the entry point -->
        <activity
            android:name=".accounts.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 2. SplashActivity -->
        <activity
            android:name=".SplashActivity"
            android:exported="false" /> <!-- 3. Onboarding Activity -->
        <activity
            android:name=".OnBoarding.OnboardingActivity"
            android:exported="false" /> <!-- 4. Signup Activity -->
        <activity
            android:name=".accounts.SignupActivity"
            android:exported="false" /> <!-- 5. Dashboard Activity -->
        <activity
            android:name=".accounts.DashboardActivity"
            android:exported="false" /> <!-- 5. Start Activity (if used) -->
        <activity
            android:name=".StartActivity"
            android:exported="false" /> <!-- 6. Camera Activity (optional, if you have a separate activity for camera) -->
        <activity
            android:name=".CameraActivity"
            android:exported="false" />
    </application>

</manifest>